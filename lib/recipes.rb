class Recipes
  # creates recipe list - the collection of all
  # each instance is a recipe
      # has a name, slug, ingredients


  # gets a recipe named
  # converts recipe name to url
  # sends url to recipe scraper
      # scraper gets ingredients from website
  # gets ingredients from scraper
  # adds everything together



#     takes user 1st level option selection and instantiates meal ideas scraper
#         returns meal ideas (to CLI interface)
#     takes user final option selection and adds to recipe list
#     compiles all recipes into one recipe list
#     takes user direct input and instantiates ingredient scraper

gets

end
